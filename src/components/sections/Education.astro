---
import Section from "../Section.astro";
import { education } from "cv.json";
---

<Section title="Education">
  <ul>
    {
      education.map(
        ({ institution, area, studyType, startDate, endDate, highlights }) => {
          const startYear = new Date(startDate).getFullYear();
          const endYear =
            endDate !== null ? new Date(endDate).getFullYear() : "Present";
          const years = `${startYear} - ${endYear}`;
          return (
            <li>
              <article>
                <header>
                  <div>
                    <h3>{institution}</h3>
                    <h4>{area}</h4>
                  </div>
                  <time>{years}</time>
                </header>
                <body>
                  <li>
                    {highlights.map((highlight) => {
                      return <ul>{highlight}</ul>;
                    })}
                  </li>
                </body>
              </article>
            </li>
          );
        },
      )
    }
  </ul>
</Section>
<style>
  ul {
    list-style-type: none;
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  article h3 {
    font-weight: 500;
  }

  body p {
    color: #d9d9d9;
    font-size: 0.8rem;
    line-height: 1.3;
    display: flex;
    flex-direction: column;
    margin-top: 5px;
  }
  article ul {
    color: #d9d9d9;
    font-size: 0.8rem;
    line-height: 1.3;
    list-style-type: none;
    display: flex;
    flex-direction: column;
    margin-bottom: 5px;
  }

  article h4 {
    font-weight: 500;
  }

  header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 4px;
  }
  time {
    font-size: 0.85rem;
  }
</style>
